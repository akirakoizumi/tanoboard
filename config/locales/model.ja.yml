ja:
  activerecord:
    models:
      user: ユーザー 
    attributes:
      user:
        id: ID
        name: 名前
        email: email
        password: パスワード(6文字以上)
        password_confirmation: パスワード（確認）
        created_at: 登録日時
        updated_at: 更新日時